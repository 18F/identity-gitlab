apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pivproxy.fullname" . }}-test-connection"
  labels:
    {{- include "pivproxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pivproxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
